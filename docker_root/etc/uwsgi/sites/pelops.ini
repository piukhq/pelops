[uwsgi]
project = pelops
base = /usr/local/src

chdir = %(base)/%(project)

module = wsgi
callable = app

master = true
processes = 1

uid = apps
gid = apps

socket = /tmp/%(project).sock
chmod-socket = 666
vacuum = true
lazy-apps = true
close-on-exec = true
enable-threads = true
